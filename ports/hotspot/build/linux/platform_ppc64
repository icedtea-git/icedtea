os_family = linux

arch = zero

arch_model = zero

os_arch = linux_zero

os_arch_model = linux_zero

lib_arch = ppc

compiler = gcc

gnu_dis_arch = ppc

sysdefs = -DLINUX -D_GNU_SOURCE -DPPC -DCC_INTERP -DZERO
