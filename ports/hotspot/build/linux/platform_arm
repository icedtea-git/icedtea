os_family = linux

arch = zero

arch_model = zero

os_arch = linux_zero

os_arch_model = linux_zero

lib_arch = arm

compiler = gcc

gnu_dis_arch = arm

sysdefs = -DLINUX -D_GNU_SOURCE -DARM -DCC_INTERP -DZERO
