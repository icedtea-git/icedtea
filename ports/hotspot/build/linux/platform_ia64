os_family = linux

arch = zero

arch_model = zero

os_arch = linux_zero

os_arch_model = linux_zero

lib_arch = ia64

compiler = gcc

gnu_dis_arch = ia64

sysdefs = -DLINUX -D_GNU_SOURCE -DIA64 -DCC_INTERP -DZERO
