os_family = linux

arch = zero

arch_model = zero

os_arch = linux_zero

os_arch_model = linux_zero

lib_arch = s390

compiler = gcc

gnu_dis_arch = s390

sysdefs = -DLINUX -D_GNU_SOURCE -DS390 -DCC_INTERP -DZERO
